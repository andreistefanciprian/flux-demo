apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: priorityclass-webhook
spec:
  interval: 1m0s
  releaseName: priorityclass-webhook
  chart:
    spec:
      chart: ./infra/priorityclass-webhook
      sourceRef:
        kind: GitRepository
        name: priorityclass-webhook
        namespace: flux-system
  values:
    image:
      repository: andreistefanciprian/k8s-priorityclass-webhook
      tag: dd193d5bb92326a6173f3e5f431a285c4e11b882-44 # {"$imagepolicy": "flux-system:priorityclass-webhook:tag"}
